import * as RequestApi from '../request';

export function getTablePeriodicInspection(url) {
    return RequestApi.getAll(url);
}

export function getAllVehiclePlates(url) {
    return RequestApi.getAll(url);
}

export function getTableAccessorySchedule(url) {
    return RequestApi.getAll(url);
}
